/**
 * doclight-display 入口（Phase 0 占位）
 *
 * 设计目标见 README.md：浏览器展示层，渲染内核输出的 HTML。
 * 功能实现（路由 / UI / 搜索 / 主题）在 Phase 1 落地。
 */
export const displayVersion = "0.1.0";

export function mount(_root: HTMLElement): void {
  // Phase 1 实现：事件总线 + 路由 + UI 挂载
}
