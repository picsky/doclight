{
  "name": "doclight-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "doclight CLI：init / dev / build / bundle / deploy（dev 已实现，其余 Phase 3）",
  "dependencies": {
    "doclight-renderer": "workspace:*"
  }
}
